import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        backgroundColor: '#fff'
    },
    heading: {
        paddingVertical: 12,
        justifyContent: 'center',
        alignItems: 'center'
    },
    headingMainText: {
        fontSize: 22,
        fontWeight: 'bold',
        color: '#2b244c'
    },
    headingSubText: {
        fontSize: 18,
        fontWeight: 'bold',
        color: '#2b244c'
    },
    headingTimeText: {
        fontSize: 12,
        fontWeight: 'bold',
        color: '#000000'
    }
})
